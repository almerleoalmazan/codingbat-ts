# Jest-TypeScript Starter Setup

## Installation

```bash
npm init -y
```

Note: Development dependencies

```bash
npm install --save-dev typescript jest ts-jest @types/jest ts-node
```

## Create jest config

```bash
npx ts-jest config:init
```
